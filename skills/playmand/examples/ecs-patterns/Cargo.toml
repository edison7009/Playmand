[package]
name = "playmand-ecs-patterns"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
publish = false
license = "MIT"

[workspace]

[dependencies]
bevy = { version = "=0.19.1", default-features = false, features = ["std", "bevy_state"] }
